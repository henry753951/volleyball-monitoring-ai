mutation CreateCorrectionDraft($submissionId: ID!) {
  createCorrectionDraft(submissionId: $submissionId) {
    id
    matchId
    setId
    annotationRevision
    annotationStatus
    processingStatus
    scoreResolutionState
    scoringCourtSide
    activeSubmissionId
  }
}
