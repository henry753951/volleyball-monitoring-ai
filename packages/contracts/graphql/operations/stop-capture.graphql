mutation StopCapture($captureSessionId: ID!) {
  stopCapture(captureSessionId: $captureSessionId) {
    id
    matchId
    sourceKind
    sourceLabel
    sourceDurationUs
    status
    health
    startedAt
    endedAt
  }
}
