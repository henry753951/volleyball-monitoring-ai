import type { PrismaClient } from '@volleyball-monitoring/db'
import { JobStatus, UserRole } from '@volleyball-monitoring/db/client'

export async function getCoachRallyReplay(
  database: PrismaClient,
  input: { rallyId: string; userId: string; role: UserRole },
) {
  const rally = await database.rally.findFirst({
    where: { id: input.rallyId, voidedAt: null, activeSubmissionId: { not: null }, ...(input.role === UserRole.ADMIN ? {} : { match: { members: { some: { userId: input.userId } } } }) },
    select: {
      id: true, matchId: true, ordinal: true, processingStatus: true,
      set: { select: { id: true, setNumber: true } },
      program: { select: { fpsNum: true, fpsDen: true } },
      activeSubmission: {
        select: {
          id: true, annotationRevision: true, submittedAt: true, scoreResolutionState: true, scoringCourtSide: true,
          leftTeam: { select: { id: true, name: true, shortName: true } }, rightTeam: { select: { id: true, name: true, shortName: true } }, scoringTeam: { select: { id: true, name: true, shortName: true } },
          keyPoints: { orderBy: { sequenceIndex: 'asc' }, select: { id: true, sequenceIndex: true, markerKind: true, isTerminal: true } },
          clipJobs: { where: { status: JobStatus.COMPLETED, clipAssetId: { not: null } }, orderBy: { completedAt: 'desc' }, take: 1, select: { id: true, actualStartCaptureUs: true, actualEndCaptureUs: true, keyPointMappings: { select: { submissionKeyPointId: true, clipPts: true, clipTimeUs: true, clipFrameIndex: true } } } },
          analysisRuns: { where: { status: JobStatus.COMPLETED }, orderBy: { activatedAt: 'desc' }, take: 1, select: {
            id: true, analysisId: true, analysisVersion: true, producerName: true, producerBuildId: true, summary: true,
            tracks: { orderBy: { trackId: 'asc' }, select: { trackId: true, courtSide: true, firstFrame: true, lastFrame: true, meanConfidence: true, identityAssignments: { orderBy: { createdAt: 'desc' }, take: 1, select: { rosterEntry: { select: { id: true, jerseyNumber: true, displayNameSnapshot: true, player: { select: { name: true } } } } } } } },
            contactEvents: { orderBy: { sequenceIndex: 'asc' }, select: { keyPointId: true, sequenceIndex: true, markerKind: true, isTerminal: true, anchorFrameIndex: true, resolvedFrameIndex: true, anchorTimeUs: true, associationState: true, ballState: true, ballFrameIndex: true, ballFrameX: true, ballFrameY: true, qualityFlags: true, actors: { select: { trackId: true, observationFrameIndex: true, associationConfidence: true, frameX1: true, frameY1: true, frameX2: true, frameY2: true, frameFootX: true, frameFootY: true, courtX: true, courtY: true, action: true } }, candidates: { orderBy: { rank: 'asc' }, select: { trackId: true, rank: true, confidence: true } }, representativePositions: { orderBy: { positionIndex: 'asc' }, select: { trackId: true, basis: true, courtX: true, courtY: true, confidence: true } } } },
            segments: { orderBy: { sequenceIndex: 'asc' }, select: { id: true, sequenceIndex: true, startKeyPointId: true, endKeyPointId: true, startFrameIndex: true, endFrameIndex: true, renderState: true, isTerminalSegment: true, qualityFlags: true, positions: { orderBy: [{ endpoint: 'asc' }, { positionIndex: 'asc' }], select: { endpoint: true, positionIndex: true, trackId: true, basis: true, courtX: true, courtY: true, confidence: true } } } },
          } },
        },
      },
    },
  })
  const submission = rally?.activeSubmission
  if (!rally || !submission) return null
  const clip = submission.clipJobs[0] ?? null
  const mappingByPoint = new Map((clip?.keyPointMappings ?? []).map(mapping => [mapping.submissionKeyPointId, mapping]))
  const analysis = submission.analysisRuns[0] ?? null
  return {
    schema_version: '1.0.0',
    rally: { id: rally.id, match_id: rally.matchId, ordinal: rally.ordinal, processing_status: rally.processingStatus.toLowerCase(), set: { id: rally.set.id, number: rally.set.setNumber }, outcome: { score_resolution: submission.scoreResolutionState.toLowerCase(), scoring_court_side: submission.scoringCourtSide?.toLowerCase() ?? null, scoring_team: submission.scoringTeam }, left_team: submission.leftTeam, right_team: submission.rightTeam },
    submission: { id: submission.id, annotation_revision: submission.annotationRevision.toString(), submitted_at: submission.submittedAt.toISOString(), key_points: submission.keyPoints.map(point => { const mapping = mappingByPoint.get(point.id); return { id: point.id, sequence_index: point.sequenceIndex, marker_kind: point.markerKind.toLowerCase(), is_terminal: point.isTerminal, clip_pts: mapping?.clipPts.toString() ?? null, clip_time_us: mapping?.clipTimeUs.toString() ?? null, clip_frame_index: mapping?.clipFrameIndex.toString() ?? null } }) },
    clip: clip && clip.actualStartCaptureUs !== null && clip.actualEndCaptureUs !== null ? { id: clip.id, url: `/api/v1/analysis/rallies/${rally.id}/clip`, duration_us: (clip.actualEndCaptureUs - clip.actualStartCaptureUs).toString(), fps: { num: rally.program.fpsNum, den: rally.program.fpsDen } } : null,
    analysis: analysis ? { id: analysis.id, analysis_id: analysis.analysisId, version: analysis.analysisVersion, producer: { name: analysis.producerName, build_id: analysis.producerBuildId }, summary: analysis.summary, tracks: analysis.tracks.map(track => { const assignment = track.identityAssignments[0]?.rosterEntry; return { track_id: track.trackId, court_side: track.courtSide.toLowerCase(), first_frame_index: track.firstFrame.toString(), last_frame_index: track.lastFrame.toString(), mean_confidence: track.meanConfidence, identity: assignment ? { roster_entry_id: assignment.id, jersey_number: assignment.jerseyNumber, name: assignment.displayNameSnapshot ?? assignment.player?.name ?? `#${assignment.jerseyNumber}` } : null } }), contact_events: analysis.contactEvents.map(event => ({ key_point_id: event.keyPointId, sequence_index: event.sequenceIndex, marker_kind: event.markerKind.toLowerCase(), is_terminal: event.isTerminal, anchor_frame_index: event.anchorFrameIndex.toString(), resolved_frame_index: event.resolvedFrameIndex?.toString() ?? null, anchor_time_us: event.anchorTimeUs.toString(), association_state: event.associationState.toLowerCase(), ball: { state: event.ballState.toLowerCase(), frame_index: event.ballFrameIndex?.toString() ?? null, frame_pos: event.ballFrameX !== null && event.ballFrameY !== null ? { x: event.ballFrameX, y: event.ballFrameY } : null }, quality_flags: event.qualityFlags, actors: event.actors.map(actor => ({ track_id: actor.trackId, observation_frame_index: actor.observationFrameIndex.toString(), association_confidence: actor.associationConfidence, frame_bbox: actor.frameX1 !== null && actor.frameY1 !== null && actor.frameX2 !== null && actor.frameY2 !== null ? { x1: actor.frameX1, y1: actor.frameY1, x2: actor.frameX2, y2: actor.frameY2 } : null, frame_foot_pos: actor.frameFootX !== null && actor.frameFootY !== null ? { x: actor.frameFootX, y: actor.frameFootY } : null, court_pos: actor.courtX !== null && actor.courtY !== null ? { x: actor.courtX, y: actor.courtY } : null, action: actor.action })), candidates: event.candidates.map(candidate => ({ track_id: candidate.trackId, rank: candidate.rank, confidence: candidate.confidence })), representative_court_positions: event.representativePositions.map(position => ({ track_id: position.trackId, basis: position.basis, court_pos: { x: position.courtX, y: position.courtY }, confidence: position.confidence })) })), paths: analysis.segments.map(segment => ({ id: segment.id, sequence_index: segment.sequenceIndex, start_key_point_id: segment.startKeyPointId, end_key_point_id: segment.endKeyPointId, start_frame_index: segment.startFrameIndex?.toString() ?? null, end_frame_index: segment.endFrameIndex?.toString() ?? null, render_state: segment.renderState.toLowerCase(), is_terminal_segment: segment.isTerminalSegment, quality_flags: segment.qualityFlags, start_court_positions: segment.positions.filter(position => position.endpoint === 'START').map(position => ({ track_id: position.trackId, basis: position.basis, court_pos: { x: position.courtX, y: position.courtY }, confidence: position.confidence })), end_court_positions: segment.positions.filter(position => position.endpoint === 'END').map(position => ({ track_id: position.trackId, basis: position.basis, court_pos: { x: position.courtX, y: position.courtY }, confidence: position.confidence })) })) } : null,
  }
}
