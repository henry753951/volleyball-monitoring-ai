
# Worker roles

The same image may run different roles selected by `WORKER_ROLE`. Jobs are durable in PostgreSQL; Redis is not the queue of record. FFmpeg/ffprobe are available only in media worker images.
