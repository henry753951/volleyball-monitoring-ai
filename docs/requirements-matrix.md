# Requirements matrix

`scaffolded`只代表目錄、contract或UI shell存在，不代表vertical slice完成。

| Flow | Contract/DB | Server/Worker | Web/PWA | Tests | Status | Exit evidence required |
|---|---|---|---|---|---|---|
| Local Traefik + PWA shell | paths/manifest defined | healthy Compose runtime; same-origin GraphQL live | installable shell/routes present | CI + Docker + headed Chrome | runtime baseline | iPad install + real WS/HLS flows |
| Match setup and membership domain | first migration + deterministic seed | strict Pothos reads/setup/swap; DB-backed membership | real list/setup/detail route and auth boundary | 19 server + 21 Web; PostgreSQL/CI/HTTP/browser E2E | verified | complete Phase 1B evidence recorded in `docs/progress.md` |
| Full-session DVR | OpenAPI 1.2.0, strict media/error schemas, TS/Python SDK, playback-window/sample-index and capture lifecycle contracts active | Typed start/stop capture, MediaMTX hook, isolated pg-boss/PostgreSQL indexer, exact bigint epoch/sample/artifact kernels, immutable MinIO, authenticated bounded HLS/resolve/step, expired-window cleanup and restart/restore smoke verified | Contract Lab workstation real HLS player, growing gap-aware timeline, bounded 3-window cache, source dialog and authoritative frame controls verified | contract/DB/media/Server/Worker/Web/SDK suites; live fMP4/MinIO, headed player and restart/restore smokes green | core full-session DVR runtime verified | production identity/TLS and 2h iPad arbitrary-seek bounded-memory acceptance |
| Annotation commands (default Z/Space/</>/?/Enter; close includes outcome) | strict WS 2.0 canonical commands + additive 2.1 soft-lock intent, immutable submission/correction and globally ordered score ledger | dedicated authorized WS and GraphQL fallback share durable idempotent domain handlers; server-derived anchors; Redis-TTL advisory edit hints; immutable submit/supersession; outcome-only geometry reuse | Contract Lab-aligned PC workstation, growing DVR timeline, reconnect outbox, presence, draggable/selectable markers, remote non-blocking edit hints, server-frame-step fine-tune and TanStack key/touch parity; Coach iPad PWA remains separate | Contract/Server/Web conflict/idempotency/close/no-op/winner/unknown/reuse/callback/soft-lock coverage; headed runtime | core workflow verified | real multi-operator production acceptance |
| Customizable keyboard bindings | ADR 0003; no wire change | command semantics remain server-validated | TanStack Vue adapter/settings implemented | recorder/conflict/reset/display/input/scope tests + headed Chrome | client verified | durable command transport must preserve configured-key/touch semantic parity |
| Clip and fake AI | AI Job 1.1.0, Result/callback 1.0.0, FlatBuffers and uv-managed Python SDK frozen | Durable clip/AI workers, canonical mapping, signed download, capability negotiation, callback validation/persistence, active-submission convergence and operator retry active | Processing/history state and authorized failed-stage retry active | Contract/SDK/Worker/Server suites; real Enter→clip→fake provider→callback→AnalysisRun runtime; duplicate/expired/bad schema/checksum/FlatBuffers/oversize callback acceptance | core Phase 4 verified | production provider credentials/secret-manager integration and real-provider acceptance |
| Coach replay/overlay/analytics | Normalized run-local tracks/events/paths, immutable artifact manifest/chunks, identity mapping and versioned saved views active | Authorized replay/analytics GraphQL plus ranged clip and windowed overlay REST active | Landscape Coach PWA replay, Canvas layers, 2D path seek, roster evidence, metrics/filters/saved views active | contract/DB/Server/Web/SDK suites; live GraphQL, HTTP 206, VOC1 manifest/chunk and headed runtime probes | core Phase 5/6 verified | real-provider court/action data and long-session iPad memory/performance acceptance |
| Phase 7 operations hardening | Restart/restore boundaries and unresolved deployment decisions documented | Component restart, PostgreSQL isolated restore and MinIO mirror/restore drills verified; durable CAS/pg-boss Outbox publisher, callback retry/failure acceptance and internal payload-free metrics/audit export covered | bounded three-window DVR and two-chunk overlay caches implemented | 2h Compose ingest soak, restart/restore drill, worker lifecycle/outbox retry tests, callback acceptance and internal-route exposure probe green | local hardening verified | production identity/TLS, scheduled off-host backup, approved retention policy, external scrape/alerts, audit retention/dashboard and physical iPad acceptance |
