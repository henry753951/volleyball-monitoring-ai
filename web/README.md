
# Nuxt web

The UI is multi-page and iPad-first. The full timeline is metadata-driven, while video and overlay data are bounded lazy-loaded windows/chunks. Browser media time is never authoritative; annotation commands include PlaybackCursor only.
